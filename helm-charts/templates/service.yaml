apiVersion: v1
kind: Service
metadata:
  name: {{ include "zerok-dashboard.fullname" . }}
  labels:
    {{- include "zerok-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zerok-dashboard.selectorLabels" . | nindent 4 }}
